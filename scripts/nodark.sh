#!/bin/bash
# Script to do numeric comparisons
var1=10
var2=20
if [ "$var2" -gt "$var1" ]
    then
        echo "$var2 is greater than $var1"
fi
# Second comparison
if [ "$var1" -gt "30"]
    then
        echo "$var is greater than 30"
    else
        echo "$var1 is less than 30"
fi
fuck=100
you=0
#valuefile = "$HOME/.xmonad/scripts/count.dat"
#capture = $(cat "$valuefile")


if test "1" -gt "10"
        echo "ass"
end
#echo " value file = ${getvalue}"
#if [$getvalue > $(cat "$Valuefile")]; then  
#	echo "fart $(cat "$Valuefile") "
#else
#	echo "ass $(cat "$Valuefile")"
#fi

